const SectionTitle = ({ children }) => {
  return (
    <div className="sticky top-0 z-50 border-b border-gray-700 bg-gray-800 px-2 py-1 text-sm">
      {children}
    </div>
  );
};

export default SectionTitle;
